<!DOCTYPE html>
<html>
<head>
    <title>Tunisian Sun</title>
</head>
<body>
<div>
    {% if is_granted('ROLE_USER') %}
    <a href="{{ path("rithis_session_logout") }}">Logout</a>
    {% else %}
    <a href="{{ path("rithis_session_login") }}">Login</a>
    {% endif %}
</div>
<hr />
{% block body %}{% endblock %}
</body>
</html>
